# MEMORY.md — Memória de Longo Prazo do Projeto

> **🔴 REGRA DE SISTEMA: A partir de agora, SEMPRE que um agente de IA concluir a implementação de uma nova feature ou refatoração complexa, ele DEVE registrar a alteração no Log de Atualizações deste arquivo. Omitir esta etapa é uma violação de protocolo.**

---

## 1. STATUS ATUAL DO PROJETO

- **Backend:** Consolidado em FastAPI com arquitetura de routers isolados, DI estrita via `dependencies.py`, exception handlers globais em `core/exceptions.py` e geração de PDFs operacional.
- **Frontend:** Modularizado em Vue 3 (Composition API). Modais extraídos para `src/components/modals/`. Composables (`useProfile`, `useToast`, `useSidebar`) ativos. Kanban, Orçamento e Portal B2C funcionais.
- **Banco de Dados:** Schema unificado com `projetos_clientes` como entidade central. RLS multi-tenant por `usuario_id`. SINAPI otimizado com RPC (`upsert_sinapi_lote`), chunking em lotes de 5k e constraint UNIQUE para deduplicação.

---

## 2. DECISÕES ARQUITETURAIS VIGENTES

### 2.1 Unificação do Domínio (Fase 11)
- **Decisão:** Eliminar tabelas fragmentadas (`obras`, `orcamentos`) e eleger `projetos_clientes` como núcleo absoluto.
- **Impacto:** Todas as FKs filhas (`orcamento_itens`, `orcamento_links`, `projetos_historico`) apontam para `projeto_id`. Rotas legadas (`/api/orcamentos`) mantidas temporariamente como aliases de compatibilidade.
- **Referência:** `fase11_padronizacao_dominio.sql`

### 2.2 Auth Nativo do Supabase (Fase 9)
- **Decisão:** Usar `auth.users` nativo do Supabase como fonte de identidade. A tabela `perfis_b2b` referencia `auth.users(id)` via FK com `ON DELETE CASCADE`.
- **Mecanismo:** Trigger `on_auth_user_created` → função `handle_new_user()` (SECURITY DEFINER) sincroniza automaticamente novos registros de `auth.users` para `perfis_b2b` extraindo `full_name` e `crea_cau` dos metadados do signup.
- **Referência:** `fase9_auth_trigger.sql`, `fase9_tenant_isolation.sql`

### 2.3 Extração de Modais no Frontend
- **Decisão:** Todo modal que antes existia inline dentro de views monolíticas (`Orcamento.vue`, `Dashboard.vue`) foi extraído para `src/components/modals/`.
- **Componentes migrados:** `EditItemModal.vue`, `SetupOrcamentoModal.vue`, `ShareModal.vue`, `CompartilhamentoModal.vue`.
- **Padrão de comunicação:** `v-model:show` para visibilidade, `emit` para ações (salvar, deletar), `props` para dados de entrada.

### 2.4 SINAPI: RPC + Chunking (Batch Processing)
- **Decisão:** Abandonar inserções linha-a-linha do SINAPI. Toda importação usa a Stored Procedure `upsert_sinapi_lote` chamada via `supabase_client.rpc()`.
- **Configuração:** Chunks de máx 5.000 registros. `SinapiBot` (pandas) processa planilhas e retorna DataFrames. Persistência é feita pelo router `sinapi.py`.
- **Deduplicação:** Constraint `uq_sinapi_itens_chave_natural` (`codigo_item`, `estado`, `mes_ano`, `desonerado`) impede duplicatas no nível do banco.
- **Referência:** `fix_duplicatas_sinapi.sql`, `sinapi_bot.py`

### 2.5 Exception Handlers com CORS Blindado
- **Decisão:** Todas as exceções não tratadas passam pelo `core/exceptions.py` que injeta headers CORS dinamicamente na resposta de erro.
- **Motivo:** O middleware CORS do Starlette/FastAPI não intercepta respostas geradas por Exception Handlers, fazendo o navegador mascarar erros 500 como falha de CORS.
- **Mapeamento:** Erros de banco (duplicate key, FK violation, RLS, timeout, JWT) são traduzidos em mensagens seguras com `error_code` semântico.

### 2.6 Papel Timbrado e White-label em Contratos
- **Decisão:** Toda proposta ou contrato emitido pelo sistema agora carrega automaticamente as marcas do tenant (construtora/engenheiro), extraídas da tabela `dados_empresa`.
- **Mecanismo:** 1. Injeção de novas variáveis contextuais (`{{logo_empresa}}`, `{{nome_fantasia_empresa}}`, `{{cnpj_empresa}}`, `{{endereco_empresa}}`) no motor de templates em `integracoes.py`. 2. Integração no `pdf_generator.py` para renderizar a logo institucional da URL no cabeçalho do PDF, restringindo a altura máxima a 15mm e aplicando um traço cinza sutil (`#e5e5e5`) divisor (estilo papel timbrado Vercel). 3. Fallback inteligente para texto caso a logo não esteja cadastrada ou falhe no download.

---

## 3. DÍVIDAS TÉCNICAS CONHECIDAS

- [ ] `routers/relatorios.py` ainda referencia a tabela legada `orcamentos` na rota de PDF comercial. Precisa migrar para `projetos_clientes`.
- [x] `routers/portal_cliente.py` referencia `orcamentos` no acesso B2C via Service Role. Precisa migrar para `projetos_clientes`. → **Resolvido:** Coluna `orcamento_id` renomeada para `projeto_id` no banco + `pin_acesso` adicionada.
- [x] A função `extrair_usuario_id_do_token()` em `routers/projetos.py` é duplicata de `get_user_id()` em `dependencies.py`. Consolidar.
- [x] Composições SINAPI (`tipo == "composicao"`) retornam `data: []` no router. Implementação concluída com precificação dinâmica.
- [ ] `supabase_schema.sql` contém o schema inicial legado com políticas permissivas `USING (true)`. Não reflete o estado atual do banco. Considerar atualizar ou depreciar.

---

## 4. LOG DE ATUALIZAÇÕES

> Formato: `[AAAA-MM-DD] — [Módulo] — Descrição resumida`

| Data | Módulo | Descrição |
| 2026-05-29 | Frontend | **Correção de Acessibilidade e Contraste de Links e Ícones (`Sidebar.vue`):** 1. Corrigida a legibilidade no Light Mode dos links inativos clicáveis (Início, Engenharia, Configurações, Sincronizar SINAPI) alterando a classe base de `text-neutral-400` para `text-neutral-600` e o estado hover para `hover:text-neutral-900` com fundo adaptativo `hover:bg-neutral-100`. 2. Aplicada a herança de cores nos ícones (`text-current`) para garantir igualdade de peso visual com o texto de cada menu. 3. Preservado o visual de links inativos/disabled em desenvolvimento ("Projetos" e "Clientes") com `text-neutral-400` discreto e cursor `cursor-not-allowed`. 4. Assegurado que todos os estados de links ativos e inativos se adaptem dinamicamente via tokens Tailwind na troca Claro/Escuro (`bg-neutral-100 dark:bg-neutral-800`). Build validado com 100% de sucesso. |
| 2026-05-29 | Frontend | **Refinamento de UI/UX e Micro-interações na Landing Page (`LandingPage.vue`, `style.css`):** 1. Transformado o menu mobile em um card flutuante centralizado e totalmente reativo ao tema (usando tokens `bg-surface`, `border-hairline`, `text-ink` e `text-ink-muted` para adaptar-se perfeitamente em Light e Dark Mode), com sombra e um overlay de fundo fosco com desfoque de fundo (`backdrop-blur-sm`). 2. Implementada animação de entrada com Scale + Fade para o menu mobile. 3. Removida redundância do botão de alternar tema de dentro do menu mobile. 4. Injetada transição de rotação leve `rotate-fade` nos ícones do toggle do hamburger menu. 5. Ativada rolagem suave global via base CSS `scroll-behavior: smooth` no `style.css`. 6. Corrigido espaçamento vertical excessivo de `py-24` para `py-16` no painel de Testemunial e Fluxo Operacional. Build validado com 100% de sucesso. |
| 2026-05-29 | Frontend | **Refatoração de Copywriting e Mockups da Landing Page (`LandingPage.vue`):** Refatorou a landing page do SaaS Vértice substituindo o conteúdo monolítico por mensagens de copywriting focadas em benefícios reais B2B2C (Vitrine do Construtor, Automação Legal via ZapSign e Transparência de Obras). Reconstruiu o Mockup Interativo com três novas abas simulando a interface real do sistema (Aba 1: Kanban & Leads com cards dinâmicos do marketplace, Aba 2: Painel de Contratos e Webhooks de Assinatura ZapSign, Aba 3: Portal do Proprietário B2C com Caixômetro e galeria de fotos de vistorias). Todas as cores absolutas Caixa foram mantidas e o build de produção finalizado com 100% de sucesso. |
| 2026-05-29 | Frontend | **Centralização de Cores Semânticas Caixa (`style.css`, `LandingPage.vue`):** Migrada a estilização da Landing Page de hexadecimais hardcoded para variáveis semânticas do Tailwind v4 (`brand-blue`, `brand-blue-hover`, `brand-orange`, `brand-orange-hover`). Registradas as novas variáveis na diretiva `@theme` nativa do `style.css`. O arquivo `LandingPage.vue` foi completamente refatorado para utilizar estas novas classes de marca de forma absoluta (sem prefixos `dark:`), preservando legibilidade e contraste e otimizando a manutenibilidade do layout. Build validado com sucesso. |
| 2026-05-29 | Frontend | **Unificação de Cores Premium Caixa na Landing Page (`LandingPage.vue`):** Corrigida a distribuição visual das cores institucionais (Laranja `#F39200` e Azul `#005CA9`) para torná-las absolutas e fixas em ambos os modos (Light e Dark). Eliminados todos os prefixos `dark:` nos botões de destaque, tags "Recomendado", ícones, bordas e textos de destaque. Botões de conversão e elementos de acento visual Caixa agora exibem a mesma intensidade e contraste vibrante e fosco independentemente do tema ativo. Build e compilação de produção validados com 100% de sucesso. |
| 2026-05-29 | Frontend | **Consolidação de Estilo e Funcionalidades na Landing Page (`LandingPage.vue`):** Redesenhada a landing page principal do SaaS "Vértice" integrando 100% das funcionalidades originais (alternância ativa de temas Claro/Escuro, links completos de rodapé/menu, ciclo anual de faturamento e portal do cliente). Aplicada a nova identidade corporativa fosca Premium Caixa (Laranja sólido `#F39200` e Azul sólido `#005CA9` no modo escuro), com remoção de glows neons e uso consistente de cards `#111111` com bordas `#262626`. Build validado com 100% de sucesso. |
| 2026-05-29 | Backend | **Papel Timbrado e White-label no Motor de Contratos (`integracoes.py`, `pdf_generator.py`):** Desenvolvida a injeção automática de dados de marca institucional do tenant (construtora/engenheiro) nos contratos gerados localmente e via integração ZapSign. O sistema agora recupera dinamicamente a logo, nome fantasia, CNPJ e endereço da tabela `dados_empresa` e injeta as variáveis correspondentes no template. Atualizado o `pdf_generator.py` para renderizar a logo no topo com altura proporcional máxima de 15mm, contorno de traço sutil e fallback limpo para o nome da empresa. |
| 2026-05-29 | Frontend | **Refatoração da Tela de Autenticação e Cadastro (`Auth.vue`):** Implementada a troca de abas fluida com transições suaves `<Transition mode="out-in" name="fade-slide">`. Adicionado efeito deslizante (underline ativo) em Laranja Caixa (`#F39200`) no indicador das abas internas com posicionamento relativo/absoluto reativo. Implementado estado de loading de submissão unificado (`isSubmitting`) no botão principal em Azul Caixa (`#005CA9` / `#004A88`) exibindo spinner Lucide (`Loader2` animado). Otimizada a reatividade responsiva do container para preencher 100% da tela em dispositivos móveis (layout full-screen, sem bordas/arredondamentos) e contrair para o formato de card flutuante a partir do breakpoint `sm` com design fosco/matte sem sombras. Build validado com 100% de sucesso. |
| 2026-05-28 | Frontend | **Reatividade de Validação de Documentos & Uniformização Estrutural (`DrawerDetalheProjeto.vue`):** 1. Implementada a reestilização estrutural do card do "Contrato Comercial" na fase `contrato_pendente` para utilizar o padrão `p-5 rounded-xl space-y-4` (mesmo design system do "Contrato de Serviço"). 2. Adicionado o sub-card reativo de "Status do Contrato" na parte superior do bloco de Contrato Comercial de forma idêntica à esteira de Engenharia. 3. Otimizado o fluxo de aprovação de documentos (`aprovarDocumento`, `validarDocumentos` e `onDocRejeitado`) para usar atribuição de spread operators (`[...docsAtualizados]`), garantindo que o Vue registre a alteração profunda de forma instantânea no Virtual DOM e renderize o card do contrato no exato milissegundo de validação de todos os documentos obrigatórios, sem necessidade de F5. Build validado com 100% de sucesso. |
| 2026-05-28 | Frontend | **Melhorias de UI/UX no Fluxo de Geração de Contratos:** 1. Reformulado o botão "Gerar Contrato de Construção" no card do Kanban (`ProjectCard.vue`) para um Icon Button (`FileText`) ao lado de "SINAPI" com tooltip reativo, prevenindo o vaziamento do card e quebra do grid. 2. Espelhado ativamente o fluxo de geração de Contrato de Serviço dentro do modal de detalhamento do projeto (`DrawerDetalheProjeto.vue`), com as devidas restrições de orçamento vazio e carregamento de templates do tipo correto. 3. Otimizado a UX do Empty State de templates exibindo um Call to Action que roteia diretamente para a tela de configurações de templates de contratos (`/configuracoes/contratos`). 4. Bloqueado o botão "Visualizar Contrato" no modal do card quando nenhum template de contrato estiver selecionado. Build validado com 100% de sucesso. |
| 2026-05-28 | Frontend | **Refatoração do Posicionamento e Animação do Menu Dropdown do Kanban (`ProjectCard.vue`):** Adicionado suporte para edge detection e inversão dinâmica baseada na altura livre do viewport no botão de 3 pontinhos. O menu de opções agora detecta se o card está localizado próximo ao limite inferior do viewport e inverte sua abertura automaticamente para cima (`bottom-full mb-1 origin-bottom-right`) em vez de abrir para baixo (`top-full mt-1 origin-top-right`). Todo o menu foi envolvido em um componente `<Transition>` nativo do Vue 3, aplicando transições de opacidade e escala no estilo Vercel (`duration-200 ease-out` / `duration-150 ease-in`). Build validado com 100% de sucesso. |
| 2026-05-28 | Frontend | **Correção de Contraste no Dark Mode da LandingPage (`LandingPage.vue`):** Corrigido o bug de legibilidade nos botões de marca no tema escuro. Onde antes era utilizado o fundo branco de marca `bg-brand-primary` conjuntamente à classe `text-white` (tornando o texto invisível), foi implementada a classe reativa `text-white dark:text-black`. A melhoria foi aplicada cirurgicamente em todos os 10 botões e links da landing page, restaurando a conformidade de acessibilidade e legibilidade. |
| 2026-05-28 | Frontend | **Restrição de Contratos por Fase (P14.3):** Implementada a restrição e distinção de documentos na esteira do `ProjectCard.vue`. Na fase `contrato_pendente` (com documentos validados), o card exibe o CTA "Gerar Proposta Comercial" filtrando apenas templates do tipo `proposta`. Na fase `engenharia_caixa`, o card exibe o CTA "Gerar Contrato de Construção" filtrando templates de `contrato`, ficando desabilitado com tooltip informativo caso o orçamento de insumos SINAPI esteja zerado. Adicionado avanço de fase reativo com limpeza automática de metadados da esteira de assinaturas ZapSign e o botão/ação "Liberar Obra" integrado no CTA principal após contrato assinado. |
| 2026-05-26 | Frontend | **Split View Modal no Detalhamento de Projetos e Sincronização Kanban:** Implementado visualizador integrado à direita do modal. Clicar em "Visualizar" expande a largura de `lg:w-[600px]` para `lg:w-[1200px]` (`transition-all duration-300`). O visualizador possui fallback clássico no mobile (`innerWidth < 1024px`). Integrados `ProjectCard.vue` e `DrawerDetalheProjeto.vue` via reatividade de props (`documento-inicial`) e watch dinâmico para abrir o Split View instantaneamente ao clicar em "Ver" diretamente no card do Kanban. Sanado o *Flexbox Blowout* no painel esquerdo aplicando a estrutura rígida no cabeçalho e encapsulando o conteúdo scrollável em uma nova div com `flex-1 overflow-y-auto min-h-0 p-6 custom-scrollbar flex flex-col gap-6`. |
| 2026-05-26 | Frontend | **Resolução de Overflow e Calibração de Tooltip no Kanban (Ações de Documentos):** Substituído o botão de recusa de documentos por um botão estilo ghost icon (`BaseButton variant="ghost" size="icon" w-7 h-7`) com tooltip reativo em `z-[100]`. O `overflow-hidden` do container da lista e da transição foi removido/ajustado dinamicamente via hooks de transição (`@after-enter` / `@before-leave` do Vue), sanando o corte físico do balão e permitindo que ele sobreponha perfeitamente qualquer elemento do Kanban. |
| 2026-05-26 | Frontend | **Solução Definitiva para Persistência da Sidebar e Transições:** Identificado o bloqueador das transições: o componente `<Sidebar />` era renderizado localmente em cada página, destruindo e recriando sua instância reativa a cada navegação e forçando a montagem estática direta. Como correção definitiva, a `<Sidebar />` foi promovida a componente global persistente no `App.vue` para rotas protegidas (fora do `<router-view>`), mantendo a mesma instância ativa entre rotas. Removidos imports e tags locais da Sidebar em `Dashboard.vue`, `EngenhariaList.vue`, `Orcamento.vue` e `Configuracoes.vue`. A transição Drill-down (`menu-slide`) agora opera com total perfeição. Build validado com 100% de sucesso. |
| 2026-05-26 | Frontend | **Refinamento de Animação Drill-down na Sidebar:** Substituído o nome da transição interna dos links de navegação para `menu-slide` em `Sidebar.vue`, implementado o efeito Slide + Fade direcional e veloz (estilo Vercel) de 15px em `style.css` e adicionada a classe `overflow-x-hidden` no container flex-col pai para evitar vazamento horizontal dos itens. Build e compilação verificados com sucesso. |
| 2026-05-26 | Frontend | **Centralização da Arquitetura de Botões (BaseButton), Micro-interações, Layout Persistente & Otimizações Light Mode:** Criado o componente reutilizável `BaseButton.vue` encapsulando variantes e tamanhos do padrão Vercel, com inversão de contraste automática em dark mode. Substituídos 100% de botões nativos soltos nos Modais, Kanban, Header e todas as abas de configurações. Ajustada a profundidade no Light Mode escurecendo `--color-canvas` para `#f4f4f5` e `--color-hairline` para `#e5e5e5` in `style.css`, evidenciando cards brancos. Injetada a classe `shadow-sm` no header (`TopHeader.vue`). Restrita a exibição do botão "+ Novo Cliente" para `route.path === '/dashboard'`. Adicionada a transição de rotas públicas `.page` (fade rápido de 150ms) no `App.vue`, desabilitando a transição nas rotas autenticadas (`requiresAuth`) para manter a Sidebar (`<aside>`) e o `TopHeader` 100% estáticos nas trocas de tela. Implementada a transição condicional `.sidebar-menu` (slide-fade direcional de 200ms com cubic-bezier) de forma interna na alternância de listas de menus de `Sidebar.vue`, sem afetar a estrutura do contêiner e o Brand header. Validação de build final com 100% de sucesso. |
| 2026-05-26 | Frontend | **Correção de Herança de Layout e Semantização:** Refatorados os contêineres principais de `Dashboard.vue` e `TopHeader.vue` (raiz e `apps/web/src/`) removendo todas as classes de cores e fundos estáticos (`bg-[#0A0A0A]`, `bg-neutral-900`, `bg-neutral-950`, `border-neutral-800`, `text-neutral-100`, etc.) que bloqueavam o tema. Aplicados os tokens semânticos (`bg-canvas`, `bg-surface`, `border-hairline`, `text-ink` e `text-ink-muted`), garantindo a inversão perfeita de contraste nos modos Claro e Escuro para o Kanban, o cabeçalho global e os cards de métricas. Validação de build final com 100% de sucesso. |
| 2026-05-26 | Frontend | **Centralização de Design System (Tailwind v4 @theme):** Calibradas e consolidadas as variáveis CSS semânticas (`bg-surface`, `bg-canvas`, `border-hairline`, `text-ink` e `text-ink-muted`) em ambos os arquivos `style.css` (raiz e `apps/web/src/`) para refletir exatamente os contrastes minimalistas da Vercel nos modos Claro e Escuro. Configurado o border-radius padrão `--radius: 8px` no tema global. Refatoradas ambas as instâncias de `ProjectCard.vue` substituindo classes utilitárias de cores estáticas por tokens semânticos nativos. Validação de build final com 100% de sucesso. |
| 2026-05-26 | Frontend | **Sincronização de Estilo Kanban (Vercel Aesthetic raiz):** Refatorados visualmente os componentes `ProjectCard.vue` e `Dashboard.vue` na pasta raiz do projeto (`src/components/`) para garantir paridade absoluta com as versões refatoradas em `apps/web/src/components/`. A alteração sanou a discrepância visual de cards de projetos brancos no Kanban do usuário através do dark mode completo, padrão minimalista, rounded-md, botões outline e dots de status circulares reativos de 8px em substituição às bordas coloridas laterais espessas. Validação de build concluída com 100% de sucesso. |
| 2026-05-26 | Frontend | **Redesenho do Dashboard/Kanban (Vercel Aesthetic):** Refatorados os componentes `Dashboard.vue`, `ProjectCard.vue` e `TopHeader.vue` aplicando a identidade visual escura e minimalista da Vercel. Fundo global alterado para o tom ultra-escuro `bg-[#0A0A0A]`, arredondamento padronizado de cards e colunas para `rounded-md`, e bordas alteradas para `border-neutral-800`. Nos cards de projetos/obras (`ProjectCard.vue`), a borda lateral colorida de status foi substituída por um dot circular reativo de 8px com cores correspondentes a cada coluna/fase. Todos os botões internos dos cards foram padronizados para outline minimalista e escuro (`h-8 rounded-md bg-neutral-950 text-neutral-300 hover:bg-neutral-850 border-neutral-800`). Na barra de progresso das medições Caixa, a altura foi reduzida para `h-1.5` com cantos `rounded-full` e preenchimento `bg-blue-500`. Empty states estilizados com borda tracejada discreta `border-neutral-800`. Modais internos refatorados para estética dark consistente (`bg-surface border-hairline rounded-md text-ink`). Build verificado com sucesso. |
| 2026-05-26 | Frontend | **Padronização Global de Modais (Vercel Style):** Criado o componente base reutilizável `BaseModal.vue` encapsulando a estética da Vercel (overlay desfocado `bg-black/45`, container `rounded-md` com borda `border-hairline` e rodapé contrastante `bg-canvas`). Migrados e refatorados com total consistência todos os modais de ação e formulário do sistema (`EditProjectModal`, `ManualItemModal`, `ConfirmarTemplateModal`, `EditItemModal`, `RejeicaoDocumentoModal`, `ShareLinkModal`, `SetupOrcamentoModal`) para o novo componente. Aplicada a reestilização estrita para os modais complexos de `ArvoreCustosModal` e `DiarioObraModal`, padronizando inputs, botões, ícones Lucide com stroke fino e arredondamentos `rounded-md`. Inserida seção 5.5 em `RULES.md` formalizando as diretrizes de design de modais. Build validado com 100% de sucesso. |
| 2026-05-26 | Frontend | **Redesign do Modal Novo Cliente (Estilo Vercel):** Modernizado o modal `NovoClienteModal.vue` aplicando a estética minimalista da Vercel. Reduzidos arredondamentos de container/inputs para `rounded-md`, eliminadas as bordas padrão dos inputs em favor de um background escuro contrastante (`bg-black/[0.04]` / `bg-neutral-800/60`) com anel de foco azul ultrafino (`focus:ring-2 focus:ring-blue-500/40`), otimizado o contraste das labels para `text-neutral-500` / `dark:text-neutral-300` para fins de acessibilidade, adicionada iconografia Lucide ultra-fina (ícones `<X>` e `<Check>` com `stroke-width` de `1.25` e `1.5`), e refinados botões do rodapé para tamanho altamente legível (`text-sm font-medium`, altura `h-9`, `px-4`) com redimensionamento proporcional dos ícones internos para `w-4 h-4`. |
| 2026-05-26 | Frontend | **Migração Global para Lucide Vue Next Concluída:** Finalizada a migração de 100% dos ícones legados do Material Symbols para a biblioteca Lucide (`lucide-vue-next`). Todos os ícones foram padronizados com traço fino (`stroke-width="1.5"`) e tamanhos responsivos do Tailwind para garantir a consistência estética premium em todas as telas públicas e privadas (`LandingPage`, `LandingSimulador`, `VitrinePublica`, `Auth`, `EstimativaWizard` e `Orcamento`). Limpeza completa de arquivos de estilo legados (`style.css`). |
| 2026-05-25 | Frontend | **Transição e Modernização de Ícones:** Corrigido o submenu no `Sidebar.vue` para usar transições de `max-height`/`opacity` altamente consistentes. Ajustado o peso de variação global do Material Symbols no `style.css` para `'wght' 300` para traços ultra-finos no estilo outline/lineart e substituídos ícones estáticos no sidebar e cabeçalho por variações modernas (`space_dashboard`, `architecture`, `edit_document`, fallback `person`). |
| 2026-05-25 | Frontend | **Edição de Insumos (type="text" + inputmode="decimal"):** Identificado e corrigido o bug nativo do navegador que selecionava o texto automaticamente. O input de quantidade e BDI foram alterados de `type="number"` para `type="text"` com `inputmode="decimal"` tanto na árvore de custos (`ArvoreCustos.vue`) quanto no modal de adição (`EditItemModal.vue`). Isso impede que as atualizações em tempo real dos cálculos reativos no Virtual DOM forcem realces azuis e seleções automáticas do cursor enquanto o usuário digita múltiplos dígitos (ex: `45`, `10`, `12`). |
| 2026-05-22 | Frontend | **Visibilidade dos Botões de Recusa:** Ocultado o botão individual "Recusar" no `DrawerDetalheProjeto.vue` quando os documentos do projeto já foram validados (`docs_validados`), garantindo consistência com o `ProjectCard.vue`. |
| 2026-05-22 | Frontend | **Indicador de Documentos Pendentes de Revisão:** Implementada uma bolinha azul pulsante no `ProjectCard.vue` e `DrawerDetalheProjeto.vue` com tooltip customizada para indicar que o projeto tem documentos completos ou parcialmente recusados/pendentes aguardando a análise do engenheiro (qualquer documento com `url` ativo na esteira `contrato_pendente` antes de `docs_validados`). |
| 2026-05-22 | Frontend | **Preservação de Documentos no Reenvio B2C:** Corrigido o `EstimativaWizard.vue` para que carregue o status dos documentos já enviados e impeça re-upload de documentos válidos/preservados, exibindo apenas o input para documentos pendentes/rejeitados com os respectivos motivos de recusa. |
| 2026-05-22 | Frontend | **Correção de Rota na Rejeição de Documentos:** Corrigida a URL do Axios no `RejeicaoDocumentoModal.vue` removendo o prefixo `/api` duplicado para alinhar com a `baseURL` global e sanar o erro 404. |
| 2026-05-22 | Frontend | **Correção de Sobreposição de Modais:** Injetado `style="z-index: ..."` inline nos modais (`RejeicaoDocumentoModal`, `SetupOrcamentoModal`, `DiarioObraModal`, `ShareLinkModal`) e no painel de detalhamento (`DrawerDetalheProjeto`) para garantir o empilhamento correto no DOM. |
| 2026-05-22 | Frontend | **Ajustes de Setup e Validação de Documentos Concluídos:** Corrigida a importação do modal SetupOrcamentoModal no `DrawerDetalheProjeto.vue` e `ProjectCard.vue` para a versão completa em `./modals/SetupOrcamentoModal.vue` e deletado o componente obsoleto. Flexibilizadas as ações de Aprovar Todos e Recusar no cofre de documentos para toda a esteira de `contrato_pendente` e corrigida a navegação condicional pós-setup. |
| 2026-05-22 | Frontend | **P12 Card Expansível e Visão Detalhada Concluído:** Criado o componente `DrawerDetalheProjeto.vue` contendo visão consolidada do projeto. Refatorado de gaveta lateral para modal responsivo (centralizado no desktop e tela cheia em mobile/tablet). Implementada visibilidade condicional do orçamento (oculto em `estimativa_enviada`), status de documentos dinâmicos ("Não solicitado" vs "Pendente"), e separação de links B2C (Jornada de Qualificação vs Portal de Obra com PIN ou bloqueado). |
| 2026-05-22 | Full-Stack | **P9.2 Rejeição de Documentos e Correção B2C Concluída:** Implementado endpoint no backend para rejeição de documentos por categoria e motivo, alterando status do projeto para docs_pendentes e registrando histórico. Frontend atualizado com modal de rejeição integrado no Kanban e reenvio de arquivos corrigidos no Portal Cliente B2C. Criados testes unitários integrados cobrindo rejeição e re-upload. |
| 2026-05-22 | Backend | **Histórico Amigável:** Expandido o dicionário `MAP_AMIGAVEL` em `projetos.py` para mapear todos os status (`docs_validados`, `em_analise_caixa`), colunas (`contrato_pendente`, `engenharia_caixa`), padrões de acabamento, booleanos (`True`/`False` para `Sim`/`Não`) e formatar chaves técnicas com dois pontos. Atualizado o `update_projeto` no backend para registrar detalhadamente no histórico todas as atualizações de propriedades do projeto. |
| 2026-05-22 | Full-Stack | **P9.1 Linha do Tempo Real Concluída:** Registro de logs na tabela `projetos_historico` no backend ao criar projeto, links de portal, documentos e contrato assinado via ZapSign. Frontend integrado no modal "Histórico e Notas" em `ProjectCard.vue` com timeline real, notas manuais e datas no padrão nacional. |
| 2026-05-22 | Full-Stack | **Remoção de Kits Básicos e Acordeão de Composições:** Removidos kits estáticos de EAP e acordeão de composições em `ArvoreCustos.vue`. Ajustadas as rotas do backend e frontend para focar em templates personalizados do usuário redimensionáveis por m². |
| 2026-05-22 | Full-Stack | **Acordeão de Composições na EAP:** Criada a sanfona de itens filhos em `ArvoreCustos.vue` com cálculo proporcional de coeficientes e subtotal de insumos, interface premium colapsável e precificação dinâmica no backend. |
| 2026-05-22 | Full-Stack | **Ajustes de Kit Básico e Precificação:** Seeding de 16 composições SINAPI e filhos no banco. Implementada a precificação dinâmica de composições no backend. Corrigidos pre-fill de padrão/tamanho no `SetupOrcamentoModal.vue` e loop infinito no modal em `Orcamento.vue`. |
| 2026-05-20 | Full-Stack | **P10 Jornada de Acesso Concluída:** Separada a jornada do lead (matchmaking B2C no wizard público) do portal do cliente ativo. O link e PIN do portal foram removidos da tela final do EstimativaWizard e agora são exibidos na Sala de Espera pós-upload de documentos. O CTA do Kanban em `engenharia_caixa` foi atualizado para "Enviar Acesso à Obra", integrando envio via WhatsApp e movendo "Abrir SINAPI" para o dropdown. Adicionados endpoints públicos GET/PATCH seguros por ID aleatório do projeto no `matchmaking.py`. |
| 2026-05-20 | Frontend | **P5.5 Fase 5 Concluída:** Criado componente premium reusável `Caixometro.vue` para progresso físico-financeiro da Caixa (PCI/PFUI) com tooltips explicativas sobre juros de evolução de obra e integrado na aba "Evolução Caixa" do portal do cliente. |
| 2026-05-20 | Testes | **P5.5 Fase 5 Concluída:** Criada suite de testes integrados e automatizados `test_portal_avancado.py` validando matchmaking, PIN parsing, rotas de feed/documentos/caixa e RLS (100% de sucesso/OK). Corrigido relacionamento da FK em `orcamento_links` no Supabase para apontar para `projetos_clientes`. |
| 2026-05-20 | Frontend | **P5.4 Fase 4 Concluída:** Linha do tempo vertical integrada no `PortalCliente.vue` consumindo dados reais de canteiro. Wizard (`EstimativaWizard.vue`) atualizado para exibir card com link do portal e PIN de segurança com botão de cópia rápida e atalho de navegação no encerramento do matchmaking B2C. |
| 2026-05-20 | Frontend | **P5.3 Fase 3 Concluída:** Portal do Cliente (`PortalCliente.vue`) refatorado com abas responsivas (Timeline, Evolução Caixa, Documentos). Modal de Compartilhamento (`ShareLinkModal.vue`) atualizado para pré-carregar e preencher o PIN com os últimos 4 dígitos do telefone do cliente. |
| 2026-05-20 | Backend | **P5.2 Fase 2 Concluída:** Criadas as rotas públicas sob `/api/portal/projetos/{hash_link}` para feed, documentos e caixa (Caixômetro físico-financeiro com cálculo simulado de juros de obra). |
| 2026-05-20 | Banco de Dados | **P5.1 Fase 1 Concluída:** Migration 002 aplicada — tabelas `obras_feed` e `projetos_documentos` criadas com RLS. Tabela `orcamento_links` corrigida: `orcamento_id` → `projeto_id`, coluna `pin_acesso` adicionada. |
| 2026-05-20 | Backend | **P5.1 Fase 1 Concluída:** Endpoint `POST /api/matchmaking/solicitar` agora gera automaticamente o link do portal e o PIN (4 últimos dígitos do telefone) na criação do Lead. |
| 2026-05-20 | Frontend | **P4.4 Concluída:** Criada Landing Page B2C (/simulador) integrada com endpoint de cálculo e redirecionamento parametrizado para o Wizard. |
| 2026-05-20 | Full-Stack | **P4.3 Concluída:** Fluxo de Matchmaking B2C operacional (Step 4 no Wizard, endpoint seguro de Lead via service_role bypass RLS, finalização anônima opcional). |
| 2026-05-19 | Backend | **P4.2 Concluída:** Criada migration SQL e endpoint público de matchmaking (/api/matchmaking) integrado com perfis_b2b. |
| 2026-05-19 | Backend | **P4.1 Concluída:** Criado endpoint público de simulação paramétrica (/api/simulador/calcular) e registrado no main.py. |
| 2026-05-19 | Frontend | **P3.4 Concluída:** Modal inline de item manual removido de FinancialSummary.vue, substituído pelo componente reutilizável ManualItemModal.vue. |
| 2026-05-19 | Frontend | **P3.3 Concluída:** Constante ETAPAS_OBRA extraída para src/constants/etapas.js e importada nos componentes ArvoreCustos.vue, EditItemModal.vue e ManualItemModal.vue. |
| 2026-05-19 | Frontend | **P3.2 Concluída:** Função formatCurrency extraída para src/utils/formatters.js e importada em todos os componentes. |
| 2026-05-19 | Banco de Dados | **P2.4 Concluída:** supabase_schema.sql depreciado com aviso de segurança. |
| 2026-05-19 | Backend | **P2.3 Concluída:** Endpoint de busca SINAPI atualizado para suportar o filtro tipo=composicao. |
| 2026-05-19 | Frontend | **P3.1 Concluída:** ShareModal e CompartilhamentoModal unificados no novo componente ShareLinkModal.vue. |
| 2026-05-19 | Backend | **P2.2 Concluída:** extrair_usuario_id_do_token consolidada com get_user_id em projetos.py usando Depends. |
| 2026-05-18 | Backend+Frontend | **P2.1 Concluída:** `portal_cliente.py` migrado de `orcamentos` → `projetos_clientes`. Model `orcamento_id`→`projeto_id`. Response key `orcamento`→`projeto`. Frontend: `ShareModal`, `CompartilhamentoModal` e `PortalCliente.vue` atualizados (payload+campos). Erros sanitizados. |
| 2026-05-18 | Frontend | **P1.3 Concluída:** URL em `FinancialSummary.vue:52` atualizada de `/orcamentos/` → `/projetos/` para alinhar com novo prefixo do backend. Bloco P1 100% completo. |
| 2026-05-18 | Backend | **P1.2 Concluída:** `relatorios.py` migrado de `orcamentos` → `projetos_clientes`. Prefixo `/api/orcamentos` → `/api/projetos`. Campos: `bdi`→`bdi_padrao`, `nome_obra`→`titulo_projeto`. Busca itens por `projeto_id`. Erro sanitizado. |
| 2026-05-18 | Backend | **P1.1 Concluída:** `gerar_contrato()` em `integracoes.py` alterada de `async def` → `def` (thread pool). Erro sanitizado conforme RULES.md §2.3. `enviar_para_zapsign()` mantida `async def`. |
| 2026-05-18 | Produto | Criação do `TODO.md` — backlog priorizado com 16 micro-tarefas (P1-P4), mapa de dependências e estimativa de esforço. |
| 2026-05-18 | Frontend | Criação do `UI_COMPONENTS.md` — catálogo de 14 componentes com props/emits, 3 composables, paleta de cores, padrões de modal/input e dívidas técnicas de UI. |
| 2026-05-18 | QA | Criação do `TEST_PLAN.md` — cenários BDD para PDF, SINAPI+BDI e Upload em Lote com critérios de aceite e matriz de riscos. |
| 2026-05-18 | Governança | Criação do `RULES.md` — fonte da verdade com regras de backend, frontend, segurança, RLS e instruções para IA. |
| 2026-05-18 | Governança | Criação do `MEMORY.md` — memória de longo prazo com decisões arquiteturais, status e log de atualizações. |
| 2026-05-18 | Banco de Dados | Consolidação documentada: unificação de domínio (Fase 11), RLS multi-tenant, RPC SINAPI com chunking, constraint de deduplicação. |
| 2026-05-14 | Backend | Correção de políticas RLS e injeção de `usuario_id` no fluxo de criação de projetos. Ajuste de prefixos de rotas (`/api/admin`, `/api/sinapi`). |
| 2026-05-14 | Backend | Refatoração do sync SINAPI: migração de inserts linha-a-linha para batch processing via Stored Procedure `upsert_sinapi_lote`. |
| 2026-05-13 | Frontend | Extração de modais do `Orcamento.vue` para `src/components/modals/` (EditItemModal, SetupOrcamentoModal, ShareModal). |
| 2026-05-13 | Banco de Dados | Implementação de RLS policies em `orcamento_itens`, `orcamento_links` e `projetos_historico` com isolamento via JOIN. |
| 2026-05-12 | Frontend | Modularização do componente Kanban (`ProjectCard.vue`). Implementação de gaveta de projetos arquivados e modal de edição. |
| 2026-05-11 | Full-Stack | Feature "Histórico e Notas": modal reativo, endpoints `GET/POST /api/projetos/{id}/historico`, tabela `projetos_historico`. |
| 2026-05-07 | Frontend | Módulo "Engenharia": `EngenhariaList.vue` com busca, filtros por status e navegação para orçamento por projeto. |
| 2026-05-06 | Infraestrutura | Deploy produção: sanitização de URLs hardcoded, `vercel.json` para SPA routing, sincronização Vercel (frontend) + Render (backend). |
